Uses "Leidenalg" clustering on five different networks to test different partition results (Cluster outputs) to directory clustering_outputs:

The following Leidenalg setting are used in "run_clusterings.py": 
CPMVertexPartition resolution setting "0.01" and "0.001" 
and ModularityVertexPartition with default parameters in use. 

Coding assisted by ChatGPT to perform loops in the directory that integrate LeidenAlg and track runtime. ChatGPT also helped with networkit library used to "Calculate various cluster metrics" in metrics_per_cluster+network.py. 

